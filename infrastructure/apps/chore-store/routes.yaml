apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: chore-store
  namespace: default
spec:
  parentRefs:
    - name: constellation-gateway
      sectionName: https-jonnowang-chore
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: chore-store
          port: 5174
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: chore-store-http-redirect
  namespace: default
spec:
  parentRefs:
  - name: constellation-gateway
    sectionName: http
  hostnames:
  - chore.jonno.wang
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
