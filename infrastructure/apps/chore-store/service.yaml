apiVersion: v1
kind: Service
metadata:
  name: chore-store
  namespace: default
  labels:
    app: chore-store
    service: chore-store
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5174
      targetPort: 3000
  selector:
    app: chore-store
