apiVersion: v1
kind: Service
metadata:
  name: chore-store
  namespace: default
  labels:
    app: chore-store
    service: chore-store
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: chore-store
